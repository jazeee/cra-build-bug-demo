{
  "name": "test-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "2.0.0-next.3e165448",
    "serve": "^9.1.0"
  },
  "scripts": {
    "start": "export NODE_PATH=src/ && react-scripts start",
    "build": "export NODE_PATH=src/ && react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "development": [
      "last 3 chrome versions"
    ],
    "production": [
      ">1%",
      "last 4 versions",
      "Firefox ESR",
      "not ie < 11"
    ]
  }
}
